<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account - Wiki AI</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <header>
        <nav>
            <a href="/" class="logo">Wiki AI</a>
            <div class="nav-links">
                <a href="/login">Login</a>
                <a href="/">Home</a>
            </div>
        </nav>
    </header>

    <main class="container" style="max-width: 500px;">
        <h1>Create Account</h1>
        <p>Join the collaborative knowledge base.</p>

        <% if (error) { %>
            <div class="error-msg"
                style="background: #ffebe9; color: #cf222e; padding: 0.8rem; border: 1px solid #d0d7de; border-radius: 4px; margin-bottom: 1.5rem;">
                <%= error %>
            </div>
            <% } %>

                <form action="/register" method="POST" class="wiki-form">
                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="text" id="username" name="username" required placeholder="Pick a username">
                    </div>

                    <div class="form-group">
                        <label for="email">Email Address</label>
                        <input type="email" id="email" name="email" required placeholder="email@example.com">
                    </div>

                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" id="password" name="password" required placeholder="Create a password">
                        <small>Passwords are securely hashed using bcrypt.</small>
                    </div>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary" style="width: 100%;">Sign Up</button>
                    </div>

                    <p style="text-align: center; margin-top: 1.5rem; font-size: 0.9rem;">
                        Already have an account? <a href="/login" style="color: var(--primary-color);">Log in</a>
                    </p>
                </form>
    </main>
</body>

</html>